# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=socialscope
POSTGRES_USER=socialscope
POSTGRES_PASSWORD=changeme

# Ollama (local LLM)
OLLAMA_BASE_URL=http://ollama:11434
LLM_MODEL=gemma4:e2b

# Scraper defaults (can be overridden from the Settings page)
MAX_SCROLLS=10

# pgAdmin (optional — enable with --profile pgadmin)
PGADMIN_EMAIL=admin@socialscope.dev
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Worker settings
WORKER_POLL_INTERVAL=30
